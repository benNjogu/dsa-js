/**
 * @stack
 * It is a LIFO data structure.
 * e.g a pile of books, a pile of plates etc
 *
 * it has four main methods:
 *  push - used to add elements to the stack
 *  pop - used to remove an element from the stack
 *  peek - used to get the very top most element in the stack
 *  length/size - used to determine the number of element in the stack
 */
